/* eslint-disable import/prefer-default-export */
/**
 * Enum for loading status.
 * @readonly
 * @enum {string}
 */
export const LoadingStatus = {
  LOADING: 'loading',
  LOADED: 'loaded',
  FAILED: 'failed',
  DENIED: 'denied',
};
